{
  "name": "@eduagent/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared contract package: zod schemas + TS types for the learner model, WS events, REST bodies, DashboardData, and MCP UI tool args",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./protocol": "./src/protocol/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
