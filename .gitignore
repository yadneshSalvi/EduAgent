plans/
.env

# video production assets (large media, never for the public repo)
videos/

# dependencies
node_modules/

# vendored monaco assets (synced from node_modules by apps/web/scripts/vendor-monaco.mjs)
apps/web/public/monaco/

# build output
.next/
dist/
build/
out/
coverage/
*.tsbuildinfo
.turbo/

# runtime data (SQLite db + per-user workspaces)
data/

# local env overrides (keep .env.example tracked)
.env.local
.env.*.local
# VPS secrets for docker compose (keep .env.production.example tracked)
.env.production

# logs & OS noise
*.log
.DS_Store
.eslintcache
