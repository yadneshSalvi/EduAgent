/**
 * Phase 1 module stub — nothing here is wired yet.
 *
 * Lands here (plans/03_backend.md §6): the `teach` and `memory` skill sources
 * (prompts/skills/…) and the per-turn mode templates (prompts/modes/*.ts),
 * with tests asserting token budget and required placeholders.
 */
export {};
