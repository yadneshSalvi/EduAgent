/**
 * Phase 1 module stub — nothing here is wired yet.
 *
 * Lands here (plans/03_backend.md §3.2–3.4):
 * - WorkspaceManager: ensureWorkspace (template + git init), readLearnerModel
 *   (zod-validated, last-known-good fallback), stateDigest.
 * - GitService: simple-git wrapper (log/show/diff/commitAll) + the memory
 *   commit grammar parser (plans/02_data_model.md §3).
 * - MemoryPipeline: post-turn checkpoint commit + memory.commit WS events.
 */
export {};
