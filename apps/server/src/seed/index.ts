/**
 * Phase 5 module stub — nothing here is wired yet.
 *
 * Lands here (plans/02_data_model.md §7): the deterministic demo seeder —
 * user "alex" with ~21 days of backdated, grammar-valid memory commits, and
 * the empty-workspace user "sam" for onboarding. Until then, root `pnpm seed`
 * is an echo no-op.
 */
export {};
