/**
 * Phase 1 module stub — nothing here is wired yet.
 *
 * Lands here (plans/03_backend.md §4): UiToolRelay — HTTP server on
 * 127.0.0.1:${RELAY_PORT}, single POST /tool-call route. Authenticates
 * per-thread session tokens (constant-time), zod-parses tool args, persists,
 * pushes WS events, and returns agent-readable instructive errors.
 */
export {};
